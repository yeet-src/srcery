#!/usr/bin/env srcery-bash

repo="${1:?usage: @dev REPO [BRANCH]}"
branch="${2:-}"

wt_path=$(@wt-create "$repo" "$branch")
wt_name=$(basename "$wt_path")

@svc-start "$wt_path" claude claude >&2

win="${wt_name}/claude"
exec tmux -L "$SRCERY_TMUX_SOCKET" attach-session -t "=srcery:=$win"

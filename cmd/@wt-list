#!/usr/bin/env srcery-bash

[[ -d "$SRCERY_DATA/worktrees" ]] || exit 0

for wt in "$SRCERY_DATA"/worktrees/*/; do
  wt="${wt%/}"
  wt="${wt##*/}"
  [[ $# -eq 0 || "$wt" == *"_${1}" ]] && echo "$wt"
done

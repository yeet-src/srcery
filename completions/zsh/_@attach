#compdef @attach
local -a candidates
candidates=(${(f)"$("$SRCERY_ROOT/completions/completers/@attach" $((CURRENT-1)) "${(@)words[2,-1]}" 2>/dev/null)"})
(( ${#candidates} )) && compadd -a candidates

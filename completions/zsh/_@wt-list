#compdef @wt-list
local -a candidates
candidates=(${(f)"$("$SRCERY_ROOT/completions/completers/@wt-list" $((CURRENT-1)) "${(@)words[2,-1]}" 2>/dev/null)"})
(( ${#candidates} )) && compadd -a candidates
